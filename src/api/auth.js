import {GET, POST} from "api";

const login = async(mail, password) => {
    try {
        const json = {
            mail : mail,
            password : password
        }
    }catch(error){

    }
}

module.export = {
    login
}